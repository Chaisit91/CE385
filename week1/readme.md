
1.1 ตอบ fristname คือ ชื่อ , age คืออายุ , mager คือ สาขา 
        ใช้ let ในการตั้งตัวแปล ชื่อ อายุ และ สาขา และ console.log เพื่อ แสดงผลลัพท์ตัวแปล ทั้ง ชื่อ อายุ และ สาขา ให้แสดงใน TERMINEL

1.2  ตอบ ตั้งตัวแปล รับ name number boolean Null undefined 
          หลังจากนั้น ใช้ console.log เพื่อแสดงค่า และใช้ typeof ในการเช็ค type ว่าจริงไหม

1.3  ตอบ ตั้งตัวแปลใช้ let ในการกำหนด มี คณิต วิทย์ อังกฤษ จากนั้น ใช้ let ในการคำนวนผลรวม คือ  คณิต + วิทย์ + อังกฤษ  และ คำนวน avg คือ ทั้งหมดที่คิด หาร 3 จากนั้น console.log เพื่อแสดงค่า ผลรวม , avg , avg >= 80

1.4 ตอบ ตั้งตัวแปลเพื่อรับ ชื่อ กับ รหัส และ อายุ ใช้ if ,else if , else เพื่อสร้างเงื่อนไข ต่าง
      if แรกใช้เช็คว่าถ้าชื่อกับรหัสตรง ก็เข้าเงื่อนไข  และแสดง  เข้าสู่ระบบสำเร็จ ,  else if ใช้ = ชื่อ หรือ รหัสผ่านไม่ตรง และแสดง ชื่อหรือรหัสผ่านไม่ถูกต้อง  ,  ส่วน else ใช้ อายุน้อยกว่า 18  และแสดง อายุไม่ถึงเกณฑ์